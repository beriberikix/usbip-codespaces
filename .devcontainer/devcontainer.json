{
	"name": "USB/IP",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"runArgs": [
		"--privileged"
	],
	// "mounts": [
	// 	"source=/lib/modules,target=/lib/modules,type=bind,consistency=cached"
	// ],
	"initializeCommand": "chmod +x .devcontainer/init.sh && .devcontainer/init.sh"
}